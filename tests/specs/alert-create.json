[
  {
    "name": "Create Alert with empty watchlist shows helpful message",
    "steps": [
      {
        "send": { "callback": "alert:create" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Your watchlist is empty — add a coin first before creating an alert."
            }
          }
        ]
      }
    ]
  }
]
