[
  {
    "name": "Manage Alerts shows empty state",
    "steps": [
      {
        "send": { "callback": "alert:manage" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "No alerts yet — tap 🔔 Create Alert to set one up."
            }
          }
        ]
      }
    ]
  }
]
