[
  {
    "name": "Configure Quiet Hours shows current status",
    "steps": [
      {
        "send": { "callback": "settings:quiet_hours" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Quiet hours are not set.\n\nSet quiet hours to suppress alerts during certain times (e.g. overnight)."
            }
          }
        ]
      }
    ]
  }
]
